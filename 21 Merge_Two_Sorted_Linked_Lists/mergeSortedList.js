(function IIFE() {
    function mergeSortedLists(list1, list2) {
        const sortedList = [];
        return sortedList;
    }
    const mergedList = mergeSortedLists()
    console.info(mergedList);
}())
